import { ParseVoivodeshipPipe } from './parse-voivodeship.pipe';

describe('ParseVoivodeshipPipe', () => {
  it('should be defined', () => {
    expect(new ParseVoivodeshipPipe()).toBeDefined();
  });
});
